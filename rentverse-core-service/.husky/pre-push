#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Color definitions
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
PURPLE='\033[0;35m'
CYAN='\033[0;36m'
WHITE='\033[1;37m'
NC='\033[0m' # No Color

# Print header
printf "${CYAN}╔══════════════════════════════════════════════════════════════════════════════╗${NC}\n"
printf "${CYAN}║${WHITE}                            🚀 PRE-PUSH VALIDATION                             ${CYAN}║${NC}\n"
printf "${CYAN}╚══════════════════════════════════════════════════════════════════════════════╝${NC}\n"
printf "\n"

# Step 1: Run linting and formatting checks
printf "${BLUE}┌──────────────────────────────────────────────────────────────────────────────┐${NC}\n"
printf "${BLUE}│${WHITE} 📝 STEP 1: Code Quality Check (Linting & Formatting)                      ${BLUE}│${NC}\n"
printf "${BLUE}└──────────────────────────────────────────────────────────────────────────────┘${NC}\n"
printf "${YELLOW}⏳ Running ESLint and Prettier checks...${NC}\n"
printf "\n"

pnpm run code:check

if [ $? -ne 0 ]; then
  printf "\n"
  printf "${RED}╔══════════════════════════════════════════════════════════════════════════════╗${NC}\n"
  printf "${RED}║${WHITE}                                ❌ FAILURE                                   ${RED}║${NC}\n"
  printf "${RED}╚══════════════════════════════════════════════════════════════════════════════╝${NC}\n"
  printf "${RED}💥 Code quality check failed!${NC}\n"
  printf "\n"
  printf "${YELLOW}💡 Quick Fix Commands:${NC}\n"
  printf "${WHITE}   • Auto-fix linting: ${CYAN}pnpm run lint:fix${NC}\n"
  printf "${WHITE}   • Auto-format code: ${CYAN}pnpm run format${NC}\n"
  printf "${WHITE}   • Fix both at once: ${CYAN}pnpm run code:fix${NC}\n"
  printf "\n"
  printf "${RED}🚫 Push aborted. Please fix the issues above and try again.${NC}\n"
  exit 1
fi

printf "${GREEN}✅ Code quality check passed!${NC}\n"
printf "\n"

# Step 2: Run security vulnerability audit
printf "${BLUE}┌──────────────────────────────────────────────────────────────────────────────┐${NC}\n"
printf "${BLUE}│${WHITE} 🔍 STEP 2: Security Vulnerability Check                                   ${BLUE}│${NC}\n"
printf "${BLUE}└──────────────────────────────────────────────────────────────────────────────┘${NC}\n"
printf "${YELLOW}⏳ Scanning for security vulnerabilities...${NC}\n"
printf "\n"

pnpm run security:check

if [ $? -ne 0 ]; then
  printf "\n"
  printf "${RED}╔══════════════════════════════════════════════════════════════════════════════╗${NC}\n"
  printf "${RED}║${WHITE}                           🚨 SECURITY ALERT                                 ${RED}║${NC}\n"
  printf "${RED}╚══════════════════════════════════════════════════════════════════════════════╝${NC}\n"
  printf "${RED}🚨 Security vulnerabilities detected!${NC}\n"
  printf "\n"
  printf "${YELLOW}🛠️  Remediation Steps:${NC}\n"
  printf "${WHITE}   • Try auto-fix: ${CYAN}pnpm audit --fix${NC}\n"
  printf "${WHITE}   • Manual review: ${CYAN}pnpm audit${NC}\n"
  printf "${WHITE}   • Documentation: ${PURPLE}https://docs.npmjs.com/auditing-package-dependencies-for-security-vulnerabilities${NC}\n"
  printf "\n"
  printf "${RED}🚫 Push aborted for security reasons. Please address vulnerabilities first.${NC}\n"
  exit 1
fi

printf "${GREEN}✅ Security vulnerability check passed!${NC}\n"
printf "\n"

# Success message
printf "${GREEN}╔══════════════════════════════════════════════════════════════════════════════╗${NC}\n"
printf "${GREEN}║${WHITE}                               🎉 SUCCESS                                    ${GREEN}║${NC}\n"
printf "${GREEN}╚══════════════════════════════════════════════════════════════════════════════╝${NC}\n"
printf "${GREEN}🎊 All pre-push validations passed successfully!${NC}\n"
printf "${CYAN}🚀 Your code is ready to be pushed to the repository.${NC}\n"
printf "\n"
printf "${PURPLE}📊 Validation Summary:${NC}\n"
printf "${WHITE}   ✓ Code linting and formatting${NC}\n"
printf "${WHITE}   ✓ Security vulnerability scan${NC}\n"
printf "\n"
printf "${YELLOW}Happy coding! 🎯${NC}\n"